spring:
  datasource:
    url: jdbc:mysql://localhost:3307/forum_user_test?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: forum123
  jpa:
    hibernate:
      # 测试库(forum_user_test)首次运行自动建表;生产/开发用 init.sql
      ddl-auto: update
    open-in-view: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/forum_content

forum:
  jwt:
    secret: "forum-system-test-secret-key-0123456789abcdef0123456789"
    expire-hours: 24

logging:
  level:
    com.forum: warn
