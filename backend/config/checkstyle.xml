<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  Forum-System 代码风格配置(宽松版)
  目标:命名规范、未使用导入、空块、行宽、禁止裸输出
  不强制:javadoc、import 顺序、final 参数(避免与 Lombok 冲突)
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <!-- 文件不允许 tabs -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- 行宽 140,忽略 import 与长 URL -->
    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^(package|import) .*|.*https?://.*"/>
    </module>

    <module name="TreeWalker">
        <!-- 命名规范 -->
        <module name="TypeName"/>
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="MemberName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="LocalVariableName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="ConstantName"/>
        <module name="PackageName"/>

        <!-- 导入检查 -->
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>

        <!-- 空块 -->
        <module name="EmptyBlock"/>

        <!-- 禁止 System.out / System.err 裸输出(日志统一走 SLF4J) -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="System\.(out|err)\."/>
            <property name="message" value="禁止使用 System.out/System.err,请使用日志框架"/>
        </module>

        <!-- 禁止空 catch 块吞异常 -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>
    </module>
</module>
